<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>node-darksky</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="head">
      <h1>Help Getting an Idea of Weather in Just a Click!</h1>
    </div> 
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Enter a City" required>
          <input type="submit" class="ghost-button" value="Get Weather">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
      <div>
        <% if(weather >=40) {%>
          <p>Precautions to be taken: temp is above 40</p>
        <%} %>
      </div>
    </div>
  </body>
</html>